@model List<UserAdd.Models.User>


@if (Model.Count == 0)
{
    <h3 class="text-error">No items matched your search query!</h3>
}
else
{
    foreach (var user in Model)
    {
        <h3>FirstName</h3>
        @user.FirstName
        
    }
}
